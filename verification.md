# UI Verification Notes

## 2026-08-16

PIN「1234」でログインし、英単語ホーム画面を確認した。上部の英単語・漢字切替、ポモドーロ、学習統計、復活チケット、カレンダー、試験日入力、お知らせ、下部4タブが表示され、実行時エラーは確認されなかった。

ログイン後画面はブラウザで実表示を確認した。下部タブは画面下端のプレビュー表示と重なる可能性があるため、実操作による画面遷移を追加で確認する。

プレビュー環境では下部固定ナビゲーションが下端のプレビュー表示と重なり、通常の座標クリックでの遷移確認ができなかった。単語帳タブのクリックイベントはDOM上から起動したため、次に遷移後の表示を確認する。

単語帳画面では「2026年度標準単語帳」と10語の英単語一覧、閲覧専用表示、発音ボタン、マイ単語帳作成入力欄が表示された。単語カードタブの遷移イベントも起動済みである。

単語カード画面で保存済み学習セット一覧が不足していたため実行時エラーを検出し、ダッシュボード応答に学習セット一覧を含めるよう修正した。型検査と自動テストは修正後も成功し、画面は開発時の自動更新でホームへ復帰した。

修正後の単語カード画面を再度表示し、エラーが消えたことを確認した。標準単語帳の選択、範囲指定、学習セット保存、空状態の案内が正常に表示されている。

管理画面では、配信・編集操作の前に管理者認証フォームが表示されることを確認した。次に、指定された固定パスワードで認証後の画面を確認する。

指定された固定パスワードを入力して認証を送信し、「管理画面を開きました」という完了表示と管理データの読み込み状態を確認した。

認証後の管理画面では、お知らせ配信、おすすめテスト配信、カレンダーイベント追加、2026年度標準単語帳編集の各フォームが表示されることを確認した。配信・更新は実データに影響するため、この確認では送信していない。

モンスター育成プレースホルダーは開発プレビューで確認する。公開版は直前の保存時点の内容であり、今回の更新は次の保存後に公開操作を行うまで反映されない。開発プレビューでは既存のPINセッションを引き継いで学習データを読み込んでいる。

ホーム画面のポモドーロの直後に「モンスター育成」「後日実装」と表示された専用の空き領域を確認した。この領域にはキャラクターや育成ロジックをまだ実装していないため、後日追加する際に既存の学習記録へ影響しない。

カレンダー領域を横スワイプ可能なページャーに置き換えた。開発プレビューではカレンダーとモンスター育成エリアの両方が読み込まれ、切替用インジケーターを操作できる。個人予定の入力欄と、直近15分の実学習記録に基づく自習室の人数表示もホーム画面で確認した。

モンスター育成エリアへの切替操作を実行した。タッチ操作では左右45px以上の移動を横スワイプとして扱い、キーボード・マウス利用者向けには同じ切替を行うインジケーターを用意している。

CSV単語帳改善後の開発プレビューを開き、単語帳タブへの切替を操作した。表示状態を再確認して、CSVテンプレート・ファイル選択・個別編集・書き出しの導線を検証する。

単語帳画面では、標準単語帳が閲覧専用として表示され、マイ単語帳の作成フォームが表示されることを確認した。CSVテンプレートと個別編集・書き出しは、作成したマイ単語帳を選択した際に表示される構成である。

## カードスワイプ演出確認

ホーム画面の開発プレビューを開き、単語カード練習画面へ移動する操作を開始した。型検査と自動テストは成功している。

単語カードタブへDOM操作で切り替え、学習セット作成フォームと保存済みセットの空状態が表示されることを確認した。

単語カード画面へ再度切り替え、標準単語帳・範囲入力・学習セット保存の導線を確認した。実データ入りセット作成と実際のスワイプ操作は、既存ユーザーデータを変更しないため未実施である。

検証用PIN「9999」でホームを再読み込みし、標準単語帳・学習ホーム・単語カード導線が利用可能な状態を確認した。

検証用学習者の単語カード画面で、標準単語帳から範囲を指定して学習セットを作成できること、おすすめテスト導線が表示されることを確認した。

検証用PINで「スワイプ動作確認」セット（標準単語帳No.1〜3）を作成する操作を行った。プレビュー上では保存フォームから保存要求まで実行したが、保存済みセット一覧への反映を待つ状態である。

API応答で検証用PINの「スワイプ動作確認」セット（No.1〜3）の保存を確認した。再読み込み後はホームへ戻ったため、単語カードタブから保存済みセットを開く操作を継続する。

保存済みセット一覧に「スワイプ動作確認 No.1〜3」が表示され、実データ入り学習セットを開ける状態を確認した。

実データ入りセットを開き、練習モードの設定画面で「通常（登録順）」「カードのスライド・裏返し演出」「すべてのカード」「練習を開始する」が表示されることを確認した。

練習セッションで実カードを開き、右方向のTouchEventスワイプを実行した。表示が「1 / 3」から「2 / 3」へ進み、前カード「努力」から次カード「挑戦」へ移動し、そのさらに次のカード「環境」がNEXT CARDとして表示された。右スワイプによるカード移動と実データのスタック表示を確認した。

同じ練習セッションで左方向のTouchEventスワイプを実行した。表示が「2 / 3」から「3 / 3」へ進み、カードが左方向の未覚え操作として次へ移動することを確認した。

実データ入りセットで練習設定の演出チェックをOFFにし、練習を開始した。演出オフでもカード、NEXT CARD、未覚え／覚えたボタン、カード進行表示が正常に表示された。

演出オフで開始した練習中に「未覚え」ボタンをクリックし、表示が2/3へ進んだ。その後「覚えた」ボタンをクリックし、表示が3/3へ進んだ。両ボタンで判定操作とカード進行が動作することを実ブラウザで確認した。
